# HmAiTranslator プロジェクトの問題点

このプロジェクトには、保守性、完全性、および構造に関するいくつかの重大な問題が存在します。

## 1. 重大: プロジェクトが不完全

**現状:**
APIとの通信を担う中心的なマクロファイルであるべき `HmGoogleGemini/HmGoogleGemini.mac` と `HmOpenAiGpt/HmOpenAiGpt.mac` がリポジトリに含まれていません。これらのファイルは他のマクロから明確に参照されていますが、実体が存在しないため、このプロジェクトは現状では動作しません。

**問題点:**
- プロジェクトが機能しないため、ユーザーはクローンまたはダウンロードしても利用できません。
- プロジェクトの最も重要な部分が欠落しており、全体像の把握や貢献が困難です。

---

## 2. 高: コードの著しい重複

**現状:**
- `HmGoogleGemini/HmAiTranslator.js` と `HmOpenAiGpt/HmAiTranslator.js` は完全に同一のファイルです。
- `HmAiTranslatorToJp.mac` や `HmAiTranslatorToEn.mac` といった、AIへの指示（プロンプト）を生成するマクロも、サービス名（`jsmode` と `aiMacroPath`）が異なるだけで、ロジックは同一です。

**問題点:**
- **保守性の低下:** 共通のロジック（例: 秀丸エディタへのテキスト挿入処理）を修正する場合、複数のファイルを個別に修正する必要があり、手間がかかる上に修正漏れやミスを引き起こす原因となります。
- **一貫性の欠如:** 将来的に片方のファイルだけが更新され、機能に差異が生まれる可能性があります。

---

## 3. 中: 複雑な構造とロジックの分断

**現状:**
- 実行フローが `*.mac` → `*.js` → `(存在しない)*.mac` というように、複数のファイルにまたがっており、追跡が困難です。
- `onRequestQuestionText` のように、`.js` ファイル内で空の関数として定義され、呼び出し元の `.mac` ファイルでその実装が提供されるという、直感的でない設計になっています。

**問題点:**
- **可読性の低下:** コードの全体像を把握しづらく、新規参入者がロジックを理解する際の障壁となります。
- **デバッグの困難化:** 問題が発生した際に、原因となっている箇所を特定するのが難しくなります。

---

## 4. 低: 設定管理の方法が不明

**現状:**
API通信のコアファイルが欠落しているため、APIキーやその他の設定（モデル名、温度設定など）がどのように管理されるべきか不明です。

**問題点:**
- **セキュリティリスク:** もしファイル内に直接APIキーを書き込む仕様だった場合、ユーザーが誤って機密情報を公開してしまうリスクがあります。
- **利便性の欠如:** 設定を変更するたびにマクロファイルを直接編集する必要があるかもしれず、利用者にとって不便です。本来であれば、設定専用のファイル（例: `.env` や `.ini`）を用意することが望ましいです。
